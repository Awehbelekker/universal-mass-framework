# ⚠️  This file is superseded by .github/workflows/deploy.yml
#
# The canonical CI/CD pipeline now lives at:
#   .github/workflows/deploy.yml
#
# Key improvements over this original draft:
#   - Workload Identity Federation replaces the deprecated service_account_key secret
#   - A 'test' job runs pytest before deploy is allowed
#   - Cloud Run is deployed with --no-allow-unauthenticated (API key auth instead)
#   - GCP_REGION is a repository variable, not hard-coded
#   - Uses uv for fast, reproducible Python dependency installation
#
# Required GitHub secrets for the new workflow:
#   GCP_PROJECT_ID
#   GCP_WORKLOAD_IDENTITY_PROVIDER
#   GCP_SERVICE_ACCOUNT
#
# Required GitHub variables:
#   GCP_REGION  (defaults to us-central1)
